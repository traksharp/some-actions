on:
  push:
    branches:
      - main

jobs:
  plan:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This is what i want to do"

  deploy:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - run: echo "I really want to deploy"